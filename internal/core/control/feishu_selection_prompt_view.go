package control

// FeishuSelectionPromptView is the generic selection-card view shape used when
// a selection flow is not yet backed by a more specific workspace/thread model.
// The final Feishu card renderer may still project it into a retained
// adapter-local DTO shape.
type FeishuSelectionPromptView = FeishuDirectSelectionPrompt
